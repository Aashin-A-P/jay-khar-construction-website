Place older or first-project images in this folder.

To show them on the website, add an archive project entry in `lib/projects.ts` with:

- `archive: true`
- `status: 'published'`
- image paths starting with `/Archives/...`
